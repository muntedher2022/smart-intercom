# 🚀 الدليل الشامل لتشغيل نظام النداء الذكي على السيرفر المحلي (Local Server)

هذا الملف يحتوي على جميع الخطوات اللازمة خطوة بخطوة لنقل النظام من جهاز المبرمج للعمل في بيئة الشركة بشكل فعلي على شبكة (الواي فاي / الكيبل).

---

## 🛠️ أولاً: المتطلبات الأساسية (تُنفذ مرة واحدة فقط على جهاز السيرفر)

يجب تنزيل وتثبيت بيئة تشغيل النظام الأساسية وهي **Node.js** على جهاز الكمبيوتر الذي سيعمل كسيرفر:
1. اذهب من متصفح السيرفر إلى الموقع الرسمي: https://nodejs.org
2. قم بتحميل النسخة المستقرة التي تحمل علامة **(LTS)**.
3. قم بتثبيت البرنامج بشكل عادي (Next -> Next -> Install).

---

## 📂 ثانياً: نقل ملفات النظام وتجهيزها

1. انسخ مجلد النظام بالكامل `smart-intercom6` وضعه في المكان المخصص على جهاز السيرفر (مثلاً: `C:\Systems\smart-intercom6` أو على سطح المكتب).
2. **خطوة هامة جداً (لمنع الأخطاء):**
   ادخل إلى المسار التالي داخل المجلد:
   `smart-intercom6/backend/`
   ابحث عن مجلد اسمه `node_modules` (إن وُجد) **وقم بحذفه تماماً**.
   *(السبب: هذا المجلد يحتوي على ملفات رُكّبت لجهاز المبرمج، وقد لا تتوافق مع نظام تشغيل السيرفر الجديد، وخصوصاً قاعدة البيانات SQLite).*

---

## ⚙️ ثالثاً: تحميل المكتبات المتوافقة مع السيرفر (NPM Install)

هذه الخطوة تضمن تحميل المكتبات المتوافقة 100% مع جهاز السيرفر الخاص بالشركة.

1. افتح مجلد `backend` الخاص بالنظام.
2. في شريط المسار بالأعلى (Address Bar)، اكتب `cmd` واضغط **Enter**. ستفتح لك شاشة سوداء موجهة تحديداً لهذا المجلد.
3. اكتب هذا الإيعاز البرمجي واضغط **Enter**:
   ```bash
   npm install
   ```
4. انتظر قليلاً (دقيقة تقريباً) حتى ينتهي النظام من تحميل وتثبيت جميع المكتبات المطلوبة للواجهة الخلفية (Backend) وقاعدة البيانات.

---

## 🟢 رابعاً: تشغيل النظام (الخطوة اليومية أو الدائمة)

بمجرد الانتهاء من التثبيت المذكور أعلاه، النظام جاهز للعمل!

1. ارجع للخلف إلى المجلد الرئيسي `smart-intercom6`.
2. انقر نقراً مزدوجاً على ملف التشغيل:
   `start.bat`
3. ستفتح شاشة سوداء. **لا تغلقها أبداً** طالما أردت أن يظل النظام يعمل للأقسام.
   *ملاحظة: إذا ظهرت لك رسالة من جدار حماية الويندوز (Windows Defender Firewall)، تأكد من وضع علامة الصح والضغط على **"سماح بالوصول" (Allow Access)** لتسمح للشبكة برؤية النظام.*

---

## 📱 خامساً: طريقة دخول الأقسام (المستخدمين) للنظام

في الشاشة السوداء التي ظهرت بعد تشغيل `start.bat`، سترى رسالة مشابهة لهذه:
🚀 السيرفر يعمل على:
   Local:   http://localhost:3000
   Network: http://192.168.1.50:3000

* **رابط الـ Network (الشبكة):** (مثل `http://192.168.1.50:3000`) هو الرابط الخاص بشركتك!
* قم بإرسال هذا الرابط لجميع الحواسيب والهواتف الذكية للأقسام (وهي متصلة بنفس شبكة الواي فاي).
* بمجرد كتابته في المتصفح سيفتح لهم النظام.

---

## 💡 نصائح ذهبية لضمان استقرار العمل:
1. **تثبيت الـ IP (Static IP):** يُفضل من إعدادات الراوتر الخاص بكم، أن يتم جعل الـ IP الخاص بجهاز السيرفر ثابتاً (Static). لماذا؟ حتى لا يتغير رقم (192.168.X.X) كلما انطفأ جهاز السيرفر، وتضطر لإرسال رابط جديد للأقسام.
2. **بدء التشغيل التلقائي:** يمكنك وضع اختصار (Shortcut) لملف `start.bat` داخل مجلد بدء التشغيل في ويندوز (Startup folder) لكي يعمل النظام تلقائياً كلما تم إعادة تشغيل جهاز السيرفر (اضغط Win+R واكتب `shell:startup` ثم ضع الاختصار هناك).

بالتوفيق في الإدارة الذكية للشركة! 🎉
