# 📱 دليل نقل ونشر النظام

## 🏠 في البيت (localhost)
```
- Frontend: http://localhost:5173
- Backend: http://localhost:3000
```

## 🏢 في الشبكة المحلية (الكمبيوتر الآخر)

### الوصول باستخدام عنوان IP:
```
- Frontend: http://<IP_OF_COMPUTER>:5173
- Backend: http://<IP_OF_COMPUTER>:3000
```

### الوصول باستخدام اسم نطاق محلي (بدون IP في الرابط) - **مُوصى به**:

لجعل الروابط تبدو أجمل وأسهل في التذكر (مثل `http://myintercom:5173/secretary` بدلاً من استخدام IP مباشرةً)، يمكنك تعديل ملف `hosts` على كل جهاز كمبيوتر (بما في ذلك جهاز الخادم وأي أجهزة عميل ستستخدم النظام).

**الخطوات على كل جهاز عميل (وخادم) يتصل بالنظام:**

1.  **اختر اسم نطاق محلي:** مثلاً، اختر `myintercom` أو `intercom.local`.
2.  **احصل على عنوان IP الخاص بجهاز الخادم:** على جهاز الخادم، افتح `Command Prompt` واكتب `ipconfig` (في Windows) أو `ifconfig` (في Linux/macOS). ابحث عن `IPv4 Address`.

3.  **عدّل ملف `hosts`:**
    *   **في نظام Windows:**
        *   افتح المفكرة (Notepad) كـ **مسؤول (Run as administrator)**.
        *   انتقل إلى `File > Open` واذهب إلى المسار: `C:\Windows\System32\drivers\etc`.
        *   في نافذة الفتح، غيّر "Text Documents (*.txt)" إلى "All Files (*.*)" حتى ترى ملف `hosts`.
        *   افتح ملف `hosts`.
        *   أضف السطر التالي في نهاية الملف (استبدل `[SERVER_IP_ADDRESS]` بعنوان IP الحقيقي للخادم واسم النطاق الذي اخترته):
            ```
            [SERVER_IP_ADDRESS] myintercom
            ```
            مثال:
            ```
            192.168.100.9 myintercom
            ```
        *   احفظ الملف.

    *   **في نظام macOS/Linux:**
        *   افتح Terminal.
        *   اكتب: `sudo nano /etc/hosts` (أو `sudo vi /etc/hosts`)
        *   أدخل كلمة مرور المسؤول.
        *   أضف السطر التالي في نهاية الملف:
            ```
            [SERVER_IP_ADDRESS] myintercom
            ```
            مثال:
            ```
            192.168.100.9 myintercom
            ```
        *   احفظ الملف واخرج من المحرر.

بعد هذه التعديلات، يمكنك الوصول إلى واجهة المدير باستخدام:
`http://myintercom:5173/`

وواجهات الأقسام باستخدام:
`http://myintercom:5173/secretary`
`http://myintercom:5173/kitchen`
إلخ.

---

## 📋 خطوات النقل والتشغيل في مكان جديد:

### الخطوة 1️⃣: تحديد عنوان IP الجديد
افتح `Command Prompt` واكتب:
```bash
ipconfig
```

ابحث عن **IPv4 Address** (مثلاً: 192.168.1.100 أو 10.0.0.5)

### الخطوة 2️⃣: انسخ المجلد كاملاً
انسخ مجلد `smart-intercom` بالكامل إلى الكمبيوتر الجديد أو على خادم

### الخطوة 3️⃣: تثبيت المتطلبات (المرة الأولى فقط)
```bash
cd backend
npm install

cd ../frontend
npm install
```

### الخطوة 4️⃣: تشغيل النظام

**الطريقة الأولى - باستخدام ملف البداية:**
```bash
cd smart-intercom
.\start.bat
```

**الطريقة الثانية - يدوياً:**
```bash
# في Terminal منفصل - الخادم الخلفي:
cd backend
npm start

# في Terminal آخر - الخادم الأمامي:
cd frontend
npm run dev
```

### الخطوة 5️⃣: الوصول من جهاز آخر
قم بفتح المتصفح من أي جهاز على نفس الشبكة واكتب:
```
http://<IP_ADDRESS>:5173
```

مثلاً: `http://192.168.1.100:5173`

---

## ✨ ما تم تحديثه (الأشياء التي تعمل تلقائياً الآن):

### ✅ socket.js
- **قبل**: استخدم IP محدد (`192.168.100.9`)
- **الآن**: يكتشف عنوان IP تلقائياً من المتصفح

### ✅ server.js  
- **قبل**: يطبع عنوان IP محدد فقط
- **الآن**: يطبع جميع عناوين IP المتاحة (localhost + Network IP)

---

## 🔧 في حالة المشاكل:

### ❌ المشكلة: لا يمكن الاتصال من جهاز آخر

**الحل:**
1. تأكد من أن الجهازين على نفس الشبكة (`ipconfig`)
2. تأكد من أن جدار الحماية لا يحجب المنافذ 3000 و5173
3. جرب `http://localhost:5173` على نفس الجهاز أولاً
4. تحقق من رسالة الخطأ في Developer Console (F12)

### ❌ المشكلة: "Address already in use"

**الحل:**
```bash
# لإغلاق العملية على MacOS/Linux:
lsof -ti:3000 | xargs kill -9
lsof -ti:5173 | xargs kill -9

# لإغلاق العملية على Windows:
netstat -ano | findstr ":3000"
taskkill /PID <PID_NUMBER> /F
```

### ❌ المشكلة: لا يتم حفظ البيانات على الأجهزة الأخرى

**الحل:**
- تأكد من أن الخادم الخلفي يعمل
- جرب تحديث الصفحة (F5)
- تحقق من Developer Console للأخطاء

---

## 📦 لنشر على خادم بعيد (AWS, DigitalOcean, إلخ):

1. انسخ كل الملفات إلى الخادم
2. في الخادم:
   ```bash
   npm install (في كلا المجلدين)
   ```
3. شغل الخادم الخلفي:
   ```bash
   cd backend
   npm start
   ```

4. للفرونتند، am تبني إصدار منتج:
   ```bash
   cd frontend
   npm run build
   # ثم رفع مجلد `dist` إلى خادم ويب
   ```

---

## 📱 ملاحظات مهمة:

- ✅ النظام يعمل على **شبكات محلية** (LAN)
- ⚠️ إذا أردت استخدام **إنترنت خارجي**، ستحتاج إلى:
  - دومين (Domain)
  - شهادة SSL
  - تكوين جدار الحماية
  - استخدام Nginx أو Apache كـ Reverse Proxy

---

**آخر تحديث:** 21 فبراير 2026
