# ☁️ دليل الرفع للاستضافة (Hosting Guide)

لرفع هذا النظام للعمل على الإنترنت، أمامك خياران رئيسيان:

## الخيار الأول: الإبقاء على SQLite (الأبسط والأسرع)
إذا كان عدد المستخدمين محدوداً (داخل مكتب أو شركة)، فإن **SQLite** كافية جداً وسريعة للغاية.
- **الاستضافة المناسبة:** استضافات VPS (مثل DigitalOcean, Hetzner, Vultr) أو استضافات Node.js (مثل Render أو Railway).
- **تثبيت المكتبات والمحرّك:**
  1. يجب أن يكون **Node.js** مثبتاً على السيرفر (نسخة 18 أو أحدث).
  2. افتح مجلد المشروع على السيرفر ونفذ الأمر التالي لتثبيت كل المكتبات المطلوبة بضغطة واحدة:
     ```bash
     npm install
     ```
  3. تنفيذ أمر بناء الواجهة الأمامية (Build):
     ```bash
     cd frontend
     npm install
     npm run build
     ```

## الخيار الثاني: التحويل إلى MySQL (للأنظمة الضخمة القومية)
إذا كنت تتوقع آلاف الطلبات المتزامنة في نفس اللحظة.
- **هل يجب التحويل؟** لا حاجة لذلك في النسخ الحالية؛ SQLite مذهلة في الأداء والسرعة.
- **خطوات التحويل:** مشروحة بالتفصيل في الملفات المرفقة للمطورين.

## 🚀 تشغيل النظام كالمحترفين (PM2)
في الاستضافات الحقيقية، لا نشغل السيرفر بالطريقة العادية، بل نستخدم أداة **PM2** لضمان استقرار النظام للأبد.
- **تثبيت PM2:**
  ```bash
  npm install -g pm2
  ```
- **بدء التشغيل:**
  ```bash
  pm2 start server.js --name "smart-intercom"
  ```
- **المميزات:** سيعمل النظام في الخلفية، وإذا تعطل السيرفر أو انقطع الكهرباء، سيعيد تشغيل نفسه تلقائياً فور عودة العمل.

## ⚠️ ملاحظة هامة جداً للرفع للإنترنت:
عند الرفع لاستضافة حقيقية (Domain)، يجب استبدال الاتصال في ملف `socket.js` و `auth.js` بـ رابط الموقع بدلاً من `localhost`. (انظر التعليقات التي أضفناها داخل هذه الملفات).

ملف auth.js في السطر الاول const API_BASE = `http://${window.location.hostname}:3000`;
ملف socket.js في السطر الخامس const SERVER_URL = `http://${window.location.hostname}:3000`;

### الخطوات العامة المقترحة للعمل:
1. شراء استضافة تدعم Node.js.
2. تفعيل شهادة الأمان **SSL (HTTPS)** (ضرورية لإصدار التنبيهات الصوتية).
3. تشغيل التطبيق عبر منفذ (Port) وتوجيه الدومين إليه عبر Nginx.
